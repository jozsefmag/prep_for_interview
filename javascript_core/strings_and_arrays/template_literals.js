//Template literals//

//Template literals are the characters with " ` " signs
//This enables string interpolation with embedded expressions

const firstName = "John"
const lastName = "Doe"
const age = 25

console.log(firstName + " " + lastName + " is " + age + " years old")

console.log(`${firstName} ${lastName} is ${age} years old`)