//Object.values()//

//This method retruns an array of a given object's own enumerable property values

const options = {
    width: 1024,
    height: 1024,
    name: "test"
}

console.log(Object.values(options))